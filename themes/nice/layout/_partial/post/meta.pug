mixin postDate(postDate)
  span.post-date= date(postDate)

mixin postTag(tags)
    if tags && tags.length
        span.post-tag 标签&nbsp;
            each tag in tags.toArray()
                a(href=url_for('tags/' + tag.name))= tag.name

mixin postComments()
     span.post-comments 评论&nbsp;
        a(href=page.permalink + '#disqus_thread') 0
